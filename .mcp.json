{
  "mcpServers": {
    "duckdb": {
      "command": "uvx",
      "args": [
        "mcp-server-motherduck",
        "--db-path",
        "./src/webapp/baller.duckdb",
        "--read-only"
      ]
    }
  }
}
